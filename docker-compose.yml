version: '3.8'

services:
  web:
    build: .
    container_name: g_scores_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./prisma/dev.db
    volumes:
      - prisma-db:/app/prisma
    restart: always

volumes:
  prisma-db:
